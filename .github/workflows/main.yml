name: "Build and Test"

on:
  push:
  pull_request:
  schedule: # Build and test daily.
    - cron: 0 11 * * *

jobs:
  build-linux:
    name: "Build (linux, ${{ matrix.arch }}, ${{ matrix.target }}) #${{ github.run_number }}"
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, x86_32]
        target: [release, debug]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Cache submodules
        uses: ./.github/actions/cache-submodules
        id: cache-submodules
        with:
          platform: linux
          arch: ${{ matrix.arch }}
          target: ${{ matrix.target }}
      - name: Pull docker images
        run: docker compose pull
        working-directory: addons/godot_xterm/native
      - name: Cache docker image layers
        uses: AndreKurait/docker-cache@0.6.0
        continue-on-error: true
        with:
          key: docker-${{ runner.os }}-${{ hashFiles('addons/godot_xterm/native/docker-compose.yml') }}
      - name: Ensure scons cache exists
        run: mkdir -p ${{github.workspace}}/.scons-cache
      - name: Build libuv
        if: steps.cache-submodules.outputs.cache-hit != 'true'
        working-directory: addons/godot_xterm/native
        run: UID_GID="$(id -u):$(id -g)" docker compose run -e TARGET=${{ matrix.target }} -e ARCH=${{ matrix.arch }} -v ${{github.workspace}}/.scons-cache:/scons-cache libuv-linux
      - name: Update documentation URLs in XML files
        run: ./misc/update_doc_urls.sh
      - name: Build libgodot-xterm
        working-directory: addons/godot_xterm/native
        run: UID_GID="$(id -u):$(id -g)" docker compose run -e TARGET=${{ matrix.target }} -e ARCH=${{ matrix.arch }} -v ${{github.workspace}}/.scons-cache:/scons-cache libgodot-xterm-linux
      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: libgodot-xterm-linux-${{ matrix.arch }}-${{ matrix.target }}
          path: addons/godot_xterm/bin/*.so

  build-other-platforms:
    name: "Build (${{ matrix.platform }}, ${{ matrix.arch }}, ${{ matrix.target}}) #${{ github.run_number }}"
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        platform: [web, macos, windows]
        target: [release, debug]
        arch: [wasm32, universal, x86_64, x86_32]
        include:
          - platform: web
            os: ubuntu-22.04
          - platform: macos
            os: macos-15
          - platform: windows
            os: windows-2022
        exclude:
          - platform: web
            arch: x86_64
          - platform: web
            arch: x86_32
          - platform: web
            arch: universal
          - platform: macos
            arch: wasm32
          - platform: macos
            arch: x86_64
          - platform: macos
            arch: x86_32
          - platform: windows
            arch: wasm32
          - platform: windows
            arch: universal
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Cache submodules
        uses: ./.github/actions/cache-submodules
        id: cache-submodules
        with:
          platform: ${{ matrix.platform }}
          target: ${{ matrix.target }}
          arch: ${{ matrix.arch }}
      - name: Cache emscripten
        if: ${{ matrix.platform == 'web' }}
        uses: actions/cache@v4
        env:
          cache-name: cache-emscripten
        with:
          path: addons/godot_xterm/native/.emcache
          key: emsdk-cache-${{ matrix.target }}-${{ hashFiles('**/*.c*', '**/*.h*') }}
          restore-keys: |
            emsdk-cache-${{ matrix.target }}-
      - name: Install web build dependencies
        if: ${{ matrix.platform == 'web' }}
        uses: pyodide/setup-emsdk@v15
        with:
          version: 3.1.14
          actions-cache-folder: emsdk-cache-${{ matrix.target }}
      - name: Install additional web build dependencies
        if: ${{ matrix.platform == 'web' }}
        run: sudo apt-get update && sudo apt-get install -y scons gcc-multilib g++-multilib
      - name: Install additional macos build dependencies
        if: ${{ matrix.platform == 'macos' }}
        run: brew install scons
      - name: Install additional windows build dependencies
        if: ${{ matrix.platform == 'windows' }}
        run: python -m pip install scons
      - name: Setup MSVC command prompt
        uses: ilammy/msvc-dev-cmd@v1
        if: ${{ matrix.platform == 'windows' }}
        with:
          arch: win${{ matrix.arch == 'x86_64' && '64' || '32' }}
      - name: Setup cmake
        if: steps.cache-submodules.outputs.cache-hit != 'true'
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: "3.23.2"
          use-32bit: ${{ matrix.arch == 'x86_32' && matrix.platform == 'windows' }}
      - name: Build libuv
        if: steps.cache-submodules.outputs.cache-hit != 'true'
        shell: bash
        env:
          PLATFORM: ${{ matrix.platform }}
          TARGET: ${{ matrix.target }}
          BITS: ${{ matrix.arch == 'x86_64' && 64 || 32 }}
        run: |
          cd addons/godot_xterm/native/thirdparty/libuv
          # Clean only CMake configuration files to avoid stale cache issues
          mkdir -p build
          rm -f build/CMakeCache.txt
          rm -rf build/CMakeFiles
          args="-DCMAKE_BUILD_TYPE=$TARGET -DBUILD_SHARED_LIBS=OFF -DCMAKE_POSITION_INDEPENDENT_CODE=TRUE \
            -DCMAKE_OSX_ARCHITECTURES=x86_64;arm64"
          if [ "$TARGET" == "release" ]; then
            args="$args -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded"
          else
            args="$args -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDebug"
          fi
          if  [ ${{matrix.platform}} == 'windows' ]; then
            # godot-cpp always builds with the non-debug version of the CRT. So we use /MT for both debug and release builds. 
            args="$args -DCMAKE_C_FLAGS_DEBUG=-MT -DCMAKE_C_FLAGS_RELEASE=-MT"
          fi
          if [ "$BITS" -eq 32 -a ${{matrix.platform}} == "windows" ]; then
            cmake -G "Visual Studio 17 2022" -A Win32 -S $(pwd) -B "build" $args
          else
            cd build
            if [ "$BITS" -eq 32 ]; then args="$args -DCMAKE_SYSTEM_PROCESSOR=i686 -DCMAKE_C_FLAGS=-m32"; fi
            cmake .. $args
            cd ..
          fi
          cmake --build build --config $TARGET
      - name: Ensure scons cache exists
        run: mkdir -p ${{github.workspace}}/.scons-cache
        shell: bash
      - name: Update documentation URLs in XML files
        run: ./misc/update_doc_urls.sh
        shell: bash
      - name: Build libgodot-xterm
        env:
          SCONS_CACHE: ${{github.workspace}}/.scons-cache
        run: |
          cd addons/godot_xterm/native
          scons platform=${{ matrix.platform }} target=template_${{ matrix.target }} arch=${{ matrix.arch }} debug_symbols=${{ matrix.platform != 'windows' && matrix.target == 'debug' && 'yes' || 'no' }} -j2
      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: libgodot-xterm-${{ matrix.platform }}-${{ matrix.arch }}-${{ matrix.target }}
          path: |
            addons/godot_xterm/bin/*.so
            addons/godot_xterm/bin/*.wasm
            addons/godot_xterm/bin/*.framework
            addons/godot_xterm/bin/spawn-helper
            addons/godot_xterm/bin/*.xcframework
            addons/godot_xterm/bin/*.dll

  web-export:
    name: "Web Export"
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - id: install-chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: 121
      - name: Setup Godot
        uses: lihop/setup-godot@v2
        with:
          version: "v4.4.1-stable"
          export-templates: true
      - name: Import assets
        run: godot --editor --headless --quit-after 100 || true
      - name: Wait for wasm build
        uses: fountainhead/action-wait-for-check@v1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: "Build (web, wasm32, release) #${{ github.run_number }}"
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Wait for linux build # Required for linux-based editor to recognize the GDExtension node types.
        uses: fountainhead/action-wait-for-check@v1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: "Build (linux, x86_64, debug) #${{ github.run_number }}"
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install binary build artifacts
        uses: actions/download-artifact@v4
        with:
          path: addons/godot_xterm/bin
          merge-multiple: true
      - name: Export for web
        shell: bash
        run: godot --no-window --export-release Web
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: test/web/package-lock.json
      - name: Smoke test Web export
        shell: bash
        working-directory: test/web
        run: |
          export GODOT_XTERM_CHROME_PATH=${{ steps.install-chrome.outputs.chrome-path }}
          npm ci
          npm test
      - name: Upload export
        uses: actions/upload-artifact@v4
        with:
          name: web-demo
          path: docs/demo

  test:
    name: Test (${{ matrix.platform }}, ${{ matrix.arch }}, ${{ matrix.godot-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        platform: [linux, macos, windows]
        arch: [x86_64, x86_32, universal]
        godot-version: ["v4.3-stable"]
        exclude:
          - platform: linux
            arch: universal
          - platform: macos
            arch: x86_64
          - platform: macos
            arch: x86_32
          - platform: windows
            arch: universal
        include:
          - platform: linux
            os: ubuntu-22.04
          - platform: macos
            os: macos-15
          - platform: windows
            os: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Setup Godot
        uses: lihop/setup-godot@v2
        with:
          bits: ${{ matrix.arch == 'x86_32' && 32 || 64 }}
          version: ${{ matrix.godot-version }}
      - name: Macos setup
        if: ${{ matrix.platform == 'macos' }}
        run: |
          # Some macos runners have metal which means we can use vulkan, others don't.
          if system_profiler SPDisplaysDataType | grep -q "Metal"; then
            echo RENDERING_DRIVER=vulkan >> .env
            brew install molten-vk
          else
            echo RENDERING_DRIVER=opengl3 >> .env
          fi
      - name: Install just
        uses: taiki-e/install-action@just
      - name: Install dependencies
        run: just install
      - name: Import assets
        shell: bash
        run: godot --editor --headless --quit-after 100 || true
      - name: Wait for build
        uses: fountainhead/action-wait-for-check@v1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: "Build (${{ matrix.platform }}, ${{ matrix.arch }}, debug) #${{ github.run_number }}"
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install binary build artifacts
        uses: actions/download-artifact@v4
        with:
          path: addons/godot_xterm/bin
          merge-multiple: true
      - name: Set spawn-helper file permissions
        if: ${{ matrix.platform == 'macos' }}
        # File permissions are not preserved by upload-artifact.
        run: chmod +x addons/godot_xterm/bin/spawn-helper
      - name: Test
        shell: bash
        run: |
          set -e # Exit on first command failure.
          set -o pipefail # Prevent tee swallowing the exit code.

          if [ "${{ matrix.platform }}" == "windows" ]; then
            # The setup-godot action does not currently support visual godot 4+ on Windows.
            just test unit | tee output.log
            just test integration | tee -a output.log
          else
            just test | tee output.log
          fi

          # This step often passes when it shouldn't, due to GUT not failing on script errors
          # (see: https://github.com/bitwes/Gut/issues/210). Therefore, we check the output
          # for expected (and unexpected) strings.
          if grep -q 'SCRIPT ERROR:' output.log || grep -q '\[ERROR\]:' output.log; then
            exit 1
          fi
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: failed-screenshots
          path: test/visual/screenshots

  benchmark:
    name: Benchmark (${{matrix.benchmark}})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        benchmark:
          [
            editor_launch,
            cursor_motion,
            dense_cells,
            light_cells,
            scrolling,
            scrolling_bottom_region,
            scrolling_bottom_small_region,
            scrolling_fullscreen,
            scrolling_top_region,
            scrolling_top_small_region,
            unicode,
          ]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Setup Godot
        uses: lihop/setup-godot@v2
        with:
          version: "v4.4.1-stable"
      - name: Install just
        uses: taiki-e/install-action@just
      - name: Update gdextension file
        run: | # Use release builds as the build job finishes sooner.
          sed -i 's/template_debug/template_release/g' addons/godot_xterm/godot-xterm.gdextension
      - name: Import assets
        shell: bash
        run: godot --editor --headless --quit-after 100 || true
      - name: Wait for build
        uses: fountainhead/action-wait-for-check@v1.2.0
        id: wait-for-build
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: "Build (linux, x86_64, release) #${{ github.run_number }}"
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Install binary build artifacts
        uses: actions/download-artifact@v4
        with:
          path: addons/godot_xterm/bin
          merge-multiple: true
      - name: Benchmark
        if: steps.wait-for-build.outputs.conclusion == 'success'
        shell: bash
        run: just bench ${{matrix.benchmark}}
        timeout-minutes: 10
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ matrix.benchmark }}
          path: benchmark/results/*.json

  process-benchmarks:
    name: Process Benchmarks
    runs-on: ubuntu-latest
    needs: [benchmark]
    permissions:
      deployments: write
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact/merge@v4
        with:
          name: benchmark-results
          pattern: "benchmark-results-*"
          delete-merged: true
      - uses: actions/download-artifact@v4
        with:
          name: benchmark-results
          path: benchmark/results/
      - name: Merge results
        run: jq -s '[.[][]]' benchmark/results/*.json > benchmark/results/all.json
      - name: Download previous benchmark data
        uses: actions/cache@v4
        with:
          path: ./cache
          key: ${{runner.os}}-benchmark
      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@v1
        if: github.ref == 'refs/heads/main' && github.event_name != 'schedule'
        with:
          tool: "customSmallerIsBetter"
          output-file-path: benchmark/results/all.json
          external-data-json-path: ./cache/benchmark-data.json
          alert-threshold: "120%"
          fail-threshold: "200%"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          comment-on-alert: true
          summary-always: true
      - name: Publish benchmark results
        if: github.ref == 'refs/heads/main' && github.event_name != 'schedule'
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: "GodotXterm Benchmarks"
          tool: "customSmallerIsBetter"
          output-file-path: benchmark/results/all.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gh-pages-branch: stable
          benchmark-data-dir-path: docs/dev/bench
          auto-push: true

  merge-artifacts:
    name: Merge Artifacts
    runs-on: ubuntu-latest
    needs: [build-linux, build-other-platforms]
    strategy:
      matrix:
        target: [release, debug]
    steps:
      - uses: actions/upload-artifact/merge@v4
        with:
          name: libgodot-xterm-${{ matrix.target }}
          pattern: "*-${{ matrix.target }}"
          delete-merged: true

  package-for-asset-lib:
    name: Package for Asset Library
    runs-on: ubuntu-22.04
    needs: [merge-artifacts]
    steps:
      - uses: actions/checkout@v4
      - name: Get version
        id: version
        run: |
          BASE_VERSION=$(grep 'version=' addons/godot_xterm/plugin.cfg | sed 's/version="\(.*\)"/\1/')
          if [[ $GITHUB_REF == refs/tags/v* ]]; then
            echo "VERSION=$BASE_VERSION" >> $GITHUB_OUTPUT
          else
            DATE=$(date +%Y%m%d)
            COMMIT_HASH=${GITHUB_SHA:0:7}
            echo "VERSION=${BASE_VERSION}-dev.${DATE}+${COMMIT_HASH}" >> $GITHUB_OUTPUT
          fi
      - name: Install just
        uses: taiki-e/install-action@just
      - name: Add binaries
        uses: actions/download-artifact@v4
        with:
          path: addons/godot_xterm/bin
          pattern: "libgodot-xterm-*"
          merge-multiple: true
      - name: Set executable permissions
        run: |
          # Set executable permissions for spawn-helper (Unix platforms)
          if [ -f addons/godot_xterm/bin/spawn-helper ]; then
            chmod +x addons/godot_xterm/bin/spawn-helper
          fi
          # Set executable permissions for macOS framework binaries
          find addons/godot_xterm/bin -name "libgodot-xterm.macos.*" -type f -exec chmod +x {} \;
      - name: Create versioned archive
        run: just package ${{ steps.version.outputs.VERSION }}
      - name: Extract package for upload # Workaround for https://github.com/actions/upload-artifact/issues/39
        run: |
          ZIP_NAME="godot-xterm-v${{ steps.version.outputs.VERSION }}.zip"
          EXTRACT_DIR="godot-xterm-v${{ steps.version.outputs.VERSION }}"
          unzip "$ZIP_NAME" -d "$EXTRACT_DIR"
      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: godot-xterm-v${{ steps.version.outputs.VERSION }}
          path: godot-xterm-v${{ steps.version.outputs.VERSION }}/
      - name: Extract release notes
        if: startsWith(github.ref, 'refs/tags/v')
        uses: ffurrer2/extract-release-notes@v2
        with:
          changelog_file: docs/CHANGELOG.md
          release_notes_file: RELEASE_NOTES.md
      - name: Create release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          prerelease: ${{ contains(steps.version.outputs.VERSION, '-') }}
          name: "v${{ steps.version.outputs.VERSION }}"
          files: godot-xterm-v${{ steps.version.outputs.VERSION }}.zip
          body_path: RELEASE_NOTES.md

  check-pre-commit:
    name: "Check Pre-Commit"
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: pre-commit/action@v3.0.1
